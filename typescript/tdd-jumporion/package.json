{
  "name": "tdd-strong",
  "version": "0.0.1",
  "description": "Strong numbers",
  "author": "CW",
  "license": "MIT",
  "keywords": [
    "tdd",
    "kata",
    "strong",
    "numbers"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json --listEmittedFiles true --diagnostics true",
    "clean": "rimraf dist && rimraf .nyc_output && rimraf coverage",
    "lint": "tslint -p tsconfig.json",
    "test": "jest"
  },
  "engines": {
    "node": ">= 4.0.0"
  },
  "devDependencies": {
    "@types/jest": "23.3.2",
    "jasmine": "^3.2.0",
    "jasmine-test-cases": "^1.0.0",
    "jest": "23.6.0",
    "ts-jest": "23.10.0",
    "tslint": "5.11.0",
    "typescript": "3.0.3"
  },
  "dependencies": {
    "@types/node": "^10.12.0"
  }
}
