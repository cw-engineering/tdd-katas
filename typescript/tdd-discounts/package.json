{
  "name": "tdd-discounts",
  "version": "0.0.1",
  "description": "Discounts calculator",
  "author": "CW",
  "license": "MIT",
  "keywords": [
    "discounts",
    "calculator"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json --listEmittedFiles true --diagnostics true",
    "clean": "rimraf dist && rimraf .nyc_output && rimraf coverage",
    "lint": "tslint -p tsconfig.json",
    "test": "jest"
  },
  "engines": {
    "node": ">= 4.0.0"
  },
  "devDependencies": {
    "@types/jest": "23.3.2",
    "jest": "23.6.0",
    "tslint": "5.11.0",
    "ts-jest": "23.10.0",
    "typescript": "3.0.3"
  }
}
